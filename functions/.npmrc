# Lokal steht legacy-peer-deps bereits in ~/.npmrc; hier fuer die CI festgehalten.
# Grund: firebase-functions 7 hat den optionalen Peer graphql ^16, das Projekt
# bleibt wegen express-graphql bei graphql 15. Ohne diese Einstellung bricht
# npm ci mit ERESOLVE ab.
legacy-peer-deps=true
